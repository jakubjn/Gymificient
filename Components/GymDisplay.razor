@* @namespace GymSystem.Components

    @if(machineData is not null)
    {
        <MudCard Style="width: 37vmin; height: 50vmin">
            <MudSkeleton Animation="Animation.False" SkeletonType=" SkeletonType.Rectangle" Height="50%" />

            <MudCardContent>
                <MudText Typo="Typo.h5">@machineData.machineName</MudText>
            </MudCardContent>

            <MudCardActions>
                <MudButton @onclick="() => ClickHandler(machineData)" Variant="Variant.Text" Color="Color.Primary">Details</MudButton>
            </MudCardActions>
        </MudCard> 
    }

@code {
    [Parameter]
    public Machine? machineData { get; set; }
    [Parameter]
    public EventCallback<Machine> OnClick { get; set; }

    private async Task ClickHandler(Machine data)
    {
        await OnClick.InvokeAsync(machineData);
    }
}
 *@